import ListIcon from "@material-ui/icons/List";

const NavIcon = () => {
  return (
      <ListIcon style={{ fontSize: "5.5vh", color: "#FFFFFF", marginRight: '4vw' }} />
  );
};

export default NavIcon;
